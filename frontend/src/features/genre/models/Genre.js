// Modelo de datos para Genre (se utiliza para mapear la respuesta de la API)
export class Genre {
  constructor(id, name, description) {
    this.id = id;
    this.name = name;
    this.description = description;
  }
}
